%
% This script reads in the latin squares of order 5 and if they are reduced
% prints them to another file 

fid = fopen('normalized_ls_order5.dat');

while ~feof(fid)
    A = fscanf (fid, '%u', [5 5]);
    if (A(1,1) == 1 && A(1,2) == 2 && A(1,3) == 3 && 
end